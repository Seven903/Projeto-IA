# Em desenvolvimento NÃO defina VITE_API_URL — o proxy do vite.config.ts
# já redireciona /api → http://localhost:3000, evitando CORS.
# Só defina em produção:
# VITE_API_URL=https://api.sigfse.com.br/api/v1